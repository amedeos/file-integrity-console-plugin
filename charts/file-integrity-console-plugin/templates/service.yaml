apiVersion: v1
kind: Service
metadata:
  {{- if not .Values.plugin.certificateSecretName }}
  annotations:
    service.beta.openshift.io/serving-cert-secret-name: {{ template "file-integrity-console-plugin.certificateSecret" . }}
  {{- end }}
  name: {{ template "file-integrity-console-plugin.name" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "file-integrity-console-plugin.labels" . | nindent 4 }}
spec:
  ports:
    - name: {{ .Values.plugin.port }}-tcp
      protocol: TCP
      port: {{ .Values.plugin.port }}
      targetPort: {{ .Values.plugin.port }}
  selector:
    {{- include "file-integrity-console-plugin.selectorLabels" . | nindent 4 }}
  type: ClusterIP
  sessionAffinity: None
